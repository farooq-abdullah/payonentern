 # My Git practice notes

Complete these answers during Exercise 1, then commit this file.

1. What does `git status` tell me?
   - Answer:i donnnn know



2. What is the difference between `git add` and `git commit`?
   - Answer:kfnvkfjdjmdslergrklkern l

3. Why should I run `git diff --staged` before committing?
   - Answer:

4. Which folder must I be in before running Git commands for this project?
   - Answer:

5. Add one Git habit you want to keep:
   - Answer:
